#include <stdio.h>
#include <stdlib.h>

int main()
{
    int val1, val2;
    int variables()
 {

 printf( "\n Ingrese dos variable: \n " );
 scanf( "%d", &val1 );
 scanf( "%d", &val2 );
 if (val1>val2) {
 printf (" La variable %d es mayor a la variable %d ",val1,val2);
 }
 if (val1<val2){
 printf (" La variable %d es mayor a la variable %d",val2,val1);
 }
 if (val1==val2){
 printf (" La variable %d y la varible %d son iguales",val1,val2);
 }

 }
 variables();
 return 0;
}
